// lib/config.dart

// For running on an Android emulator, the host machine's localhost is 10.0.2.2.
// For a physical device, this should be the local IP address of the machine running the backend.
const String baseUrl = 'http://10.0.2.2:5000';